{
  "name": "rowrm",
  "version": "0.0.4",
  "description": "Lightweight \"\"\"ORM\"\"\" for when you don't need joins.",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "test": "ts-node src/tests.ts",
    "start": "ts-node-dev --transpile-only --respawn --no-notify src/tests.ts",
    "build": "mkdir -p dist/ ; tsc",
    "preublishOnly": "npm run build"
  },
  "author": "Pete Hunt <floydophone@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@databases/sqlite": "^2.0.0",
    "@types/tape": "^4.13.0",
    "@types/tape-async": "^2.3.1",
    "tape": "^5.0.1",
    "tape-async": "^2.3.0",
    "ts-node": "^8.10.2",
    "ts-node-dev": "^1.0.0-pre.56",
    "typescript": "^3.9.7"
  },
  "optionalDependencies": {
    "@databases/sqlite": "^2.0.0"
  },
  "dependencies": {
    "@databases/sql": "^2.1.0",
    "tiny-invariant": "^1.1.0"
  }
}
